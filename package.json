{
  "name": "memorylayer-arena",
  "version": "1.0.0",
  "private": true,
  "description": "Render deployment for the MemoryLayer Trust Arena demo engine",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm install -g @raajvamsy/memorylayer",
    "start": "memorylayer start --host 0.0.0.0 --port ${PORT:-8080} --data /data"
  }
}
